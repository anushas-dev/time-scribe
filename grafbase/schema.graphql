type Book @model {
  id: ID!
  title: String!
  genre: [Genre]
  isbn: ID!
  asin: ID
  author: [Author]
}

type Author @model {
  id: ID!
  firstName: String!
  lastName: String!
}

type Genre @model {
  id: ID!
  name: String!
}